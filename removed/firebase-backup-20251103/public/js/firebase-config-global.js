// Backup of original public/js/firebase-config-global.js — preserved before removal
// DO NOT EDIT THIS FILE. Original global config shim backed up on 2025-11-03.
// ---- BEGIN ORIGINAL ----
// Non-module global wrapper that exposes FIREBASE_CONFIG for older scripts
(function(){
  try { if (window.__MVSG_FIREBASE_CONFIG__) { window.FIREBASE_CONFIG = window.__MVSG_FIREBASE_CONFIG__; return; } } catch(e){}
  try {
    const meta = document.querySelector('meta[name="firebase-config"]');
    if (meta && meta.content) {
      try { window.FIREBASE_CONFIG = JSON.parse(meta.content); } catch(e) { /* ignore parse */ }
    }
  } catch(e){}
  try {
    const cached = localStorage.getItem('FIREBASE_CONFIG');
    if (cached) {
      try { window.FIREBASE_CONFIG = JSON.parse(cached); } catch(e) { /* ignore */ }
    }
  } catch(e){}
  window.FIREBASE_CONFIG = window.FIREBASE_CONFIG || {
    // original backup stub content
  };
})();
// ---- END ORIGINAL ----
