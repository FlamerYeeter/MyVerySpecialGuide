// Firebase config removed: stub to avoid importing Firebase SDKs in this build.
// If other modules import { app, db } we export nulls so callers can handle absence.
export const app = null;
export const db = null;